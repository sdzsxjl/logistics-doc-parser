不久前，一家物流公司找到他希望能帮忙用AI优化业务流程，他于是转型创业，成为了FDE。

"我发现他们每天的工作就是重复做一件事：客户发过来一个PDF，他们手动把单号、收发地址抠出来，再录入到Excel里。"Lawted说，这家公司有四五十人，其中二三十人都在干这类重复性的工作。

他用AI编程工具搭了一个demo（演示版软件）。原来需要人工花几分钟才能处理的一份PDF，AI几秒钟就解析完了。他说，在看到这样的效率提升后，这家物流公司便和他签订了意向合同。

---

## 物流单据智能解析系统 — 项目概述

### 项目结构

```
f:\code\业务流程\
├── app.py                  # Streamlit Web界面（主入口）
├── requirements.txt        # Python依赖
├── .env.example            # API Key配置模板
├── .gitignore
├── src/
│   ├── __init__.py
│   ├── models.py           # 数据模型（Pydantic）
│   ├── pdf_extractor.py    # PDF文本提取（pdfplumber）
│   ├── ai_parser.py        # Claude API智能解析
│   ├── regex_parser.py     # 正则规则兜底解析
│   └── excel_exporter.py   # Excel导出（openpyxl）
├── sample_pdfs/            # 测试用PDF样本
│   ├── test_waybill_01.pdf # 顺丰运单示例
│   └── test_waybill_02.pdf # 中通面单示例
└── output/                 # 生成的Excel文件
```

### 启动方式

```
cd f:\code\业务流程
streamlit run app.py
```

浏览器打开 http://localhost:8501

### 核心功能

| 功能 | 说明 |
|------|------|
| PDF文本提取 | 基于pdfplumber，支持文本块+表格提取 |
| AI智能解析 | Claude API + 结构化JSON输出，智能识别各字段 |
| 正则兜底 | 无需API Key，支持顺丰/中通/圆通/京东/DHL/UPS等常见格式 |
| Excel导出 | 带样式的xlsx文件（表头冻结、交替行色、自动列宽） |
| 批量处理 | 一次上传多个PDF，逐文件解析 |
| 在线编辑 | 结果表格支持在线修改校对后再导出 |
| CSV导出 | 同时支持CSV格式下载 |

### 提取的字段

运单号、订单号、寄件人（姓名/电话/地址/省/市/区）、收件人（姓名/电话/地址/省/市/区）、发货日期、重量、件数、运输方式、承运商、申报价值、付款方式、备注

### 使用方式

- 有API Key：在左侧栏输入 Anthropic API Key，选择 "AI优先" 模式，AI智能解析效果最佳
- 无API Key：自动切换为"仅正则解析"模式，对常见格式的运单也能较好地提取

正则解析器已验证可以准确提取顺丰、中通等常见物流公司运单的关键字段。AI模式下（配置Claude API Key）能处理更多样的PDF格式。

---

## 事件驱动 vs 手动上传

现在的系统是"人手动上传 → 解析"的模式，但实际场景应该是：

客户发邮件/微信 → PDF到达 → 系统自动识别 → 自动解析 → 自动录入Excel

这才是真正的效率提升。常见的事件驱动方案：

| 方式 | 适用场景 |
|------|----------|
| 监听邮箱 | 客户发PDF到指定邮箱，自动下载解析 |
| 监听文件夹 | 客户把PDF传到共享文件夹，自动处理 |
| API接口 | 客户系统直接调用接口传PDF |

### 微信场景分析

微信确实是中国物流场景里最主流的客户沟通渠道：

- **个人微信**：没有官方API，第三方方案（如 itchat）会被封号，不适合商用。
- **企业微信（推荐）**：有官方API，客户发PDF到企业微信 → 回调通知 → 自动下载 → 解析 → 入库。但企业微信需要公司注册认证，对接周期长。

**实际最实用的做法**：加一个文件监听模式。

客户微信发的PDF → 人工存到一个共享文件夹 → 系统自动检测新文件 → 自动解析 → 自动追加到Excel

```
客户微信发PDF → 员工保存到 D:\运单待处理\ → 系统自动处理 → Excel自动更新
```

这样：
- 不需要人工打开网页上传
- 不需要改客户习惯（客户还是用微信发）
- 系统在后台持续运行，新文件到达自动处理
- 配合已有的 Streamlit 界面做结果查看

### 微信自动下载

微信电脑版本身就有自动下载文件的功能。设置路径：微信左下角菜单 → 设置 → 文件管理 → 文件自动下载（开启）。

微信收到的文件会自动保存到：
```
C:\Users\<用户名>\Documents\WeChat Files\<微信号>\FileStorage\File\
```

或者也可以自定义下载目录，比如指向 `D:\运单待处理\`。

**全自动链路**：客户微信发PDF → 微信自动下载到指定文件夹 → 系统检测到新文件 → 自动解析 → 自动追加到Excel，完全不需要人工操作。

---

## 文件夹监听功能

侧边栏有两种工作模式：

| 模式 | 说明 |
|------|------|
| 📤 手动上传 | 原来的上传方式 |
| 👁️ 文件夹监听 | 自动检测文件夹中新PDF |

### 特性
- **自动去重**：同一文件不会重复处理
- **自动追加**：新运单数据追加到已有Excel，不会覆盖
- **实时日志**：页面显示每步处理状态
- **后台运行**：浏览器开着即可持续运行

---

## 监听原理详解

整个监听机制分三层：

### 1. 底层：操作系统事件监听

`src/folder_watcher.py` 使用 `watchdog` 库，它直接对接 Windows 的文件系统 API（`ReadDirectoryChangesW`）。

```
Windows文件系统
    │
    ▼
watchdog.Observer  ─── 后台线程，操作系统通知 "有新文件了"
    │
    ▼
PDFHandler.on_created()  ─── 收到事件，触发回调
```

关键逻辑：`Observer` 在后台启动线程，调用 `schedule` 注册监听路径。**不是轮询！** 不需要每隔几秒去扫一遍文件夹。操作系统主动推送文件变更事件，CPU零开销。

### 2. 防抖与去重

文件写入不是一瞬间完成的。微信下载一个PDF可能要几百毫秒，期间文件系统可能触发多次事件：

```
微信开始写文件 → 触发 on_created
微信继续写入...
微信写完         → 可能再触发 on_modified
```

防御措施：

| 问题 | 解决方案 | 说明 |
|------|----------|------|
| 文件还在写入中 | 延迟 400ms 再读文件 | Timer(100ms) + sleep(300ms) |
| 同一文件反复触发 | 文件名+大小去重，跳过已处理的 | 记录 `文件名:文件大小` 作为唯一标识 |
| 临时文件干扰 | 忽略 `~` 和 `.` 开头的文件 | 这些是编辑器/系统的临时文件 |

### 3. 回调链路

```
新PDF检测到
    │
    ▼
on_new_pdf(filepath)          ← 在 app.py 里定义的回调
    │
    ├─ 读取文件字节
    ├─ 提取PDF文本 (pdfplumber)
    ├─ AI解析 或 正则解析
    ├─ 结果追加到 st.session_state（线程安全加锁）
    └─ 自动写入Excel (append_to_excel)
```

回调中使用 `threading.Lock()` 保护共享数据，确保 watchdog 的后台线程和 Streamlit 的前端线程不冲突。

### 4. UI 刷新

Streamlit 本身是请求-响应模型，不会自动刷新。监听模式下页面每5秒自动刷新页面，拉取最新的解析结果显示在表格里。

### 整体架构图

```
┌─────────────────────────────────────────────────────┐
│  微信客户端                                          │
│  客户发PDF → 自动下载到 D:\运单待处理\                │
└────────────────────┬────────────────────────────────┘
                     │ 文件写入完成
                     ▼
┌─────────────────────────────────────────────────────┐
│  Windows 文件系统 ──► ReadDirectoryChangesW API      │
└────────────────────┬────────────────────────────────┘
                     │ 事件通知
                     ▼
┌─────────────────────────────────────────────────────┐
│  watchdog Observer（后台线程）                        │
│  ├─ 防抖延迟 400ms                                   │
│  ├─ 去重检查（文件名+大小）                            │
│  └─ 触发回调 on_new_pdf()                            │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│  回调处理（app.py）                                   │
│  ├─ pdfplumber 提取文本                              │
│  ├─ Claude AI / 正则 解析字段                        │
│  ├─ threading.Lock 保护共享数据                       │
│  ├─ 追加到 st.session_state                          │
│  └─ 自动写入 Excel（openpyxl）                        │
└────────────────────┬────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────┐
│  Streamlit UI（前端线程）                             │
│  ├─ 每5秒自动刷新                                    │
│  ├─ 显示处理日志                                     │
│  └─ 可编辑结果表格 + 下载按钮                         │
└─────────────────────────────────────────────────────┘
```

核心思路：操作系统告诉你文件来了 → 等它写完 → 解析 → 入库 → 界面刷新，全程自动。

---

## pdfplumber 提取原理

pdfplumber 底层做了三种不同粒度的提取：

### 方式一：`page.extract_text()` — 整页文本

原理：pdfplumber 拿到页面中所有字符对象（每个字符的 x/y 坐标、字体、大小），按规则排序重组：

```
PDF 原始数据（无结构）:
  char "张" at (72, 500)
  char "三" at (84, 500)
  char "收" at (200, 460)
  ...

        ↓ 按 y 坐标分行，按 x 坐标排序

重组后的文本:
  张三     ← y≈500 这一行
  收货人   ← y≈460 这一行
  ...
```

核心算法按坐标把散落的字符拼成人类阅读顺序的文本。这比直接读 PDF 原始数据流要准得多，因为 PDF 内部的字符顺序可能是乱的。

### 方式二：`page.extract_words()` — 逐词 + 坐标

pdfplumber 根据字符间距判断哪些字符组成一个"词"（中文按字间距，英文按空格），然后给出每个词在页面上的精确坐标：

```
┌────────────────────────────────────────────┐
│  PDF 页面 (A4, 595×842 pt)                  │
│                                            │
│  运单号：SF1234567890123   ← y=750          │
│  ↑72,750          ↑200,750                 │
│                                            │
│  寄件人：张三              ← y=700          │
│  ↑72,700    ↑150,700                       │
│                                            │
│  地址：广东省深圳市...       ← y=670         │
│  ↑72,670                                  │
└────────────────────────────────────────────┘

提取到的 text_blocks:
[
  {"text": "运单号：",  "x0": 72,  "y0": 748, "x1": 130, "y1": 762},
  {"text": "SF1234567890123", "x0": 200, "y0": 748, "x1": 320, "y1": 762},
  {"text": "寄件人：", "x0": 72,  "y0": 698, "x1": 130, "y1": 712},
  {"text": "张三",     "x0": 150, "y0": 698, "x1": 170, "y1": 712},
  ...
]
```

坐标信息的价值在于——可以判断"哪个值是哪个标签的"：标签和值在同一行（y坐标相近），值在标签右边（x坐标更大）。判断逻辑：

- 同一行：`abs(y0差值) < 5px`
- 水平相邻：值的 x0 > 标签的 x1，且间距 < 50px

### 方式三：`page.extract_tables()` — 表格提取

原理：pdfplumber 检测页面上的线条（水平线和垂直线），推断出表格的网格结构，然后把每个单元格里的文本填进去：

```
PDF 里的表格线:                   提取结果:
┌──────┬──────┬──────┐          [["运单号","寄件人","日期"],
│运单号│寄件人│日期  │           ["SF123","张三","1-15"],
├──────┼──────┼──────┤           ["ZT456","李四","1-16"]]
│SF123 │张三  │1-15  │
├──────┼──────┼──────┤
│ZT456 │李四  │1-16  │
└──────┴──────┴──────┘
```

即使没有表格线（无线表格），pdfplumber 也能根据文本对齐规律推断。

### 总结

| 方法 | 返回 | 用途 |
|------|------|------|
| `extract_text()` | 纯文本字符串 | 喂给 AI / 正则做字段提取 |
| `extract_words()` | 词列表 + 坐标 | 精确的标签-值配对（需要知道位置关系） |
| `extract_tables()` | 二维数组 | 运单中的表格数据（费用明细等） |

三种方法都在 `PDFExtractor.extract()` 中调用，`extract_text()` 的结果传给 AI 解析，`extract_words()` 和 `extract_tables()` 作为结构化补充，表格数据也会拼接进 full_text 一起发给 AI。
